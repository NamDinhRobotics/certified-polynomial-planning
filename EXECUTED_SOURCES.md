Historical protocols keep their original hashes. Current src contains the corrected APIs.
The executed_sources/<sha256>/<filename> files preserve the exact earlier versions.
A verifier accepts only a file with the recorded hash, never the nearest/current version.
The package manifest independently binds all current and archived files.
Broad workspace provenance may list unrelated modules outside this minimal export;
this archive preserves shipped dependency versions needed for the recorded evidence.
